package com.trainingplanner.domain;

/**
 * Supported 1RM estimation formulas.
 * Values match the data model (§4.1 preferredOneRmFormula).
 */
public enum OneRmFormula {
    EPLEY,
    BRZYCKI,
    LOMBARDI
}
